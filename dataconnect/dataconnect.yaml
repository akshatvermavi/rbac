specVersion: "v1beta"
serviceId: "rbac"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rbac-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
